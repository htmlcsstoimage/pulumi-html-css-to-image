name: Release
on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]
concurrency:
  group: release
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  prepare:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_repository.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      publish: ${{ steps.version.outputs.publish }}
      create_tag: ${{ steps.version.outputs.create_tag }}
      tag: ${{ steps.version.outputs.tag }}
    env:
      GOWORK: 'off'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}
      - name: Check release version
        id: version
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          version="$(cat VERSION)"
          if [[ ! "$version" =~ ^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-(alpha|beta|rc)\.(0|[1-9][0-9]*))?$ ]]; then
            echo 'VERSION must contain a semantic version such as 0.1.0 or 0.1.0-beta.1, without a v prefix.' >&2
            exit 1
          fi
          tag="v${version}"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          releases="$(gh api --paginate "repos/${GITHUB_REPOSITORY}/releases?per_page=100" --jq '.[] | [.tag_name, .draft] | @tsv')"
          while IFS=$'\t' read -r release_tag draft; do
            if [[ "$release_tag" == "$tag" && "$draft" == false ]]; then
              echo "$tag is already published; leaving it unchanged."
              echo 'publish=false' >> "$GITHUB_OUTPUT"
              exit 0
            fi
          done <<< "$releases"
          create_tag=true
          if git rev-parse --verify --quiet "refs/tags/${tag}" >/dev/null; then
            create_tag=false
            if [[ "$(git rev-list -n 1 "$tag")" != "$(git rev-parse HEAD)" ]]; then
              echo "$tag belongs to another commit. Rerun its original release workflow or bump VERSION." >&2
              exit 1
            fi
          fi
          echo "create_tag=$create_tag" >> "$GITHUB_OUTPUT"
          echo 'publish=true' >> "$GITHUB_OUTPUT"
  sdks:
    needs: prepare
    if: needs.prepare.outputs.publish == 'true'
    name: SDK / ${{ matrix.language }}
    strategy:
      fail-fast: false
      matrix:
        language: [nodejs, python, dotnet, go, java]
    uses: ./.github/workflows/sdk.yaml
    with:
      language: ${{ matrix.language }}
      ref: ${{ github.event.workflow_run.head_sha }}
  plugin:
    needs: [prepare, sdks]
    if: needs.prepare.outputs.publish == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      GOWORK: 'off'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Create version tag
        if: needs.prepare.outputs.publish == 'true' && needs.prepare.outputs.create_tag == 'true'
        env:
          TAG: ${{ needs.prepare.outputs.tag }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git tag --annotate "$TAG" --message "Release $TAG"
          git push origin "refs/tags/${TAG}"
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Check existing plugin release assets
        id: assets
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.prepare.outputs.tag }}
        run: node scripts/check-release-assets.mjs
      # Run here: tags created with GITHUB_TOKEN do not start another workflow.
      - name: Build release
        if: steps.assets.outputs.complete != 'true'
        uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7.2.3
        with:
          version: v2.18.2
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ github.token }}
  publish-nodejs:
    needs: plugin
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: https://registry.npmjs.org
      - uses: actions/download-artifact@v4
        with:
          name: sdk-nodejs
      - name: Extract tested SDK
        run: tar -xzf sdk-nodejs.tar.gz
      - name: Publish npm package with trusted publishing
        shell: bash
        run: |
          set -euo pipefail
          version="$(cat VERSION)"
          status="$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' "https://registry.npmjs.org/@html-css-to-image%2fpulumi/${version}")"
          case "$status" in
            200) echo "npm version ${version} already exists" ;;
            404)
              tag=latest
              [[ "$version" != *-* ]] || tag=next
              npm publish sdk/nodejs/bin --access public --provenance --tag "$tag"
              ;;
            *) echo "npm version lookup failed: HTTP $status" >&2; exit 1 ;;
          esac
  publish-python:
    needs: plugin
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: actions/download-artifact@v4
        with:
          name: sdk-python
      - name: Extract tested SDK
        run: tar -xzf sdk-python.tar.gz
      - name: Publish Python package with trusted publishing
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: sdk/python/dist/
          skip-existing: true
  publish-dotnet:
    needs: plugin
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - uses: actions/download-artifact@v4
        with:
          name: sdk-dotnet
      - name: Extract tested SDK
        run: tar -xzf sdk-dotnet.tar.gz
      - name: NuGet trusted publishing login
        uses: NuGet/login@v1
        id: nuget
        with:
          user: ${{ secrets.NUGET_USER }}
      - name: Publish NuGet package
        env:
          NUGET_API_KEY: ${{ steps.nuget.outputs.NUGET_API_KEY }}
        run: bash scripts/publish-nuget.sh
  publish-java:
    needs: plugin
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
          cache-dependency-path: packaging/java/pom.xml
          server-id: central
          server-username: MAVEN_CENTRAL_USERNAME
          server-password: MAVEN_CENTRAL_PASSWORD
      - uses: actions/download-artifact@v4
        with:
          name: sdk-java
      - name: Extract tested SDK
        run: tar -xzf sdk-java.tar.gz
      - name: Check Maven Central version
        id: maven
        run: |
          set -euo pipefail
          version="$(cat VERSION)"
          status="$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' "https://repo.maven.apache.org/maven2/com/htmlcsstoimage/pulumi/${version}/pulumi-${version}.pom")"
          case "$status" in
            200) echo 'publish=false' >> "$GITHUB_OUTPUT" ;;
            404) echo 'publish=true' >> "$GITHUB_OUTPUT" ;;
            *) echo "Maven Central version lookup failed: HTTP $status" >&2; exit 1 ;;
          esac
      - name: Import Maven signing key
        if: steps.maven.outputs.publish == 'true'
        uses: crazy-max/ghaction-import-gpg@v6
        with:
          gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.GPG_PASSPHRASE }}
      - name: Publish Java SDK to Maven Central
        if: steps.maven.outputs.publish == 'true'
        env:
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
        run: mvn --batch-mode --no-transfer-progress -f sdk/java/pom.xml -Prelease deploy
  publish-go:
    needs: plugin
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: actions/download-artifact@v4
        with:
          name: sdk-go
      - name: Extract tested SDK
        run: tar -xzf sdk-go.tar.gz
      - name: Publish Go SDK branch and module tag
        run: bash scripts/publish-go-sdk.sh
  finalize:
    needs: [prepare, plugin, publish-nodejs, publish-python, publish-dotnet, publish-java, publish-go]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Publish completed release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ needs.prepare.outputs.tag }}
        run: gh release edit "$TAG" --draft=false
